# requirements.txt
#
# [Anaconda Prompt] pip install -r requirements.txt --break-system-packages
# (the --break-system-packages flag is for Linux-inside-the-VM only, where
#  Tor/Splash setup pip-installs into a system Python — see setup/linux_vm/.
#  On the Windows host with Anaconda, just: pip install -r requirements.txt)
#
# Windows-specific install notes (see README "Windows package gotchas"
# section for the full explanation of why each of these needs an extra step):
#   - pyzbar:        needs the Visual C++ Redistributable for VS 2013 (x64)
#                     installed separately — the DLL itself ships in the wheel.
#   - pytesseract:    this is just the Python wrapper; Tesseract the *engine*
#                     is a separate Windows installer (see README).
#   - psycopg2-binary: no extra step needed on Windows/Anaconda — the binary
#                     wheel bundles libpq, unlike on some minimal Linux images.

# Core
requests[socks]==2.32.3
python-dotenv==1.0.1
# Parsing / extraction
beautifulsoup4==4.12.3
PGPy13==0.6.1rc1
base58==2.1.1
pyzbar==0.1.9
Pillow==10.4.0
pytesseract==0.3.13
nltk==3.9.1
# Blockchain / graph
google-cloud-bigquery==3.25.0
pandas==2.2.2
numpy==1.26.4
networkx==3.3
# ML / explainability
scikit-learn==1.5.1
shap==0.46.0
# Storage / DB
psycopg2-binary==2.9.9
neo4j==5.23.0
redis==5.0.8
minio==7.2.8
# API / UI
fastapi==0.112.2
uvicorn==0.30.6
streamlit==1.38.0
plotly==5.24.0
jellyfish==1.1.0
# LLM narration backends (pluggable — see services/llm/brief.py)
google-genai==0.3.0
openai==1.51.0
